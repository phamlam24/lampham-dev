import { Update } from "app/classes/about_website/Changelog";

class VersionCardProps {
    version: Update;
    className: string
}

export default function VersionCard({version, className}: VersionCardProps) {
    return <></> 
}